# AMUCS Nexus - shared

Genuinely shared contracts used by multiple subsystems, and nothing else.

- `types` - shared type definitions.
- `constants` - shared constants.
- `schemas` - shared data schemas/validation contracts.

This is not a dumping ground. If a definition is used by only one subsystem, it belongs in that subsystem.

