[project]
name = "amucs-nexus-ingestion"
version = "0.1.0"
description = "AMUCS Nexus ingestion: acquire and process official source material."
requires-python = ">=3.10"
dependencies = [
  "beautifulsoup4>=4.12",
  "pypdf>=4.0",
  "httpx>=0.27",
]

[tool.pytest.ini_options]
testpaths = ["tests"]