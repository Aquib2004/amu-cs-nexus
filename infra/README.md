# AMUCS Nexus - Infra

Deployment and infrastructure assets. Modular monolith; no Kubernetes or microservices.

- `docker` - Dockerfiles and container assets (later phase).
- `nginx` - reverse proxy / TLS configuration (later phase).
- `scripts` - deployment helper scripts (later phase).

Nothing here is implemented yet.

